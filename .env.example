# Configuración existente
JWT_SECRET=your_super_secret_jwt_key_here
MYSQL_DATABASE=menucom_db
MYSQL_PORT=3306
MYSQL_PASSWORD=your_mysql_password
MYSQL_USER=your_mysql_user
MYSQL_HOST=localhost
POSTGRESQL_URL=postgresql://user:password@localhost:5432/menucom_db
ENV=development

# Configuración de Firebase para autenticación social

# MÉTODO 1: Archivo de configuración local (RECOMENDADO para desarrollo)
# Crea un archivo 'menucom-gconfig.json' en la raíz del proyecto
# con las credenciales descargadas desde Firebase Console
# ¡IMPORTANTE! Este archivo se ignora automáticamente en git

# MÉTODO 2: Variables de entorno (RECOMENDADO para producción)
# Descomenta y configura estas variables si no usas el archivo JSON:
# FIREBASE_PROJECT_ID=menucom-ff087
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-fbsvc@menucom-ff087.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nTU_CLAVE_PRIVADA_AQUI\n-----END PRIVATE KEY-----\n"

# Orígenes permitidos para CORS (HTTP y WebSocket)
# Separados por coma. Ej: http://localhost:3000,https://midominio.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# MercadoPago Webhook Secret (obligatorio en producción)
MP_WEBHOOK_SECRET=your_mp_webhook_secret_here

# Secreto para firmar QR de tickets (obligatorio en producción, mínimo 32 chars)
TICKET_QR_SECRET=your-secure-random-key-min-32-chars

# Nota: 
# 1. La FIREBASE_PRIVATE_KEY debe mantenerse en una sola línea con \n para los saltos de línea
# 2. Asegúrate de que esté entre comillas dobles
# 3. No compartas estas credenciales públicamente
# 4. En producción, usa variables de entorno seguras (Render Env Vars, Azure Key Vault, AWS Secrets Manager, etc.)
